# 旅行规划小助手 — 技术方案（已实现版）

> 文档版本：v1.1 · 对应代码仓库 `travel-plan-assistant`  
> 智能能力由 **DeepSeek API** 驱动；支持 **CLI 命令行** 与 **Web 平台** 双模式。

---

## 一、项目背景与定位

### 1.1 业务痛点

| 痛点 | 表现 |
|------|------|
| 信息零散 | 景点、美食、交通、预算分散在多个渠道 |
| 决策繁琐 | 天数有限，手工排期易漏项、折返、节奏不合理 |
| 经验不足 | 不清楚预约规则、季节准备、当地消费水平 |

### 1.2 产品定位

**旅行规划垂直领域助手**，聚焦自然语言规划与结构化攻略交付：

- 用户用**自然语言**描述需求（如「杭州两日游，轻松一点」）
- 系统生成**完整攻略**（9 大模块），支持**追问式最小范围修订**
- 非旅行问题**拒答并引导**
- 支持 **CLI 本地导出** 与 **Web 多会话、流式对话、分享**

### 1.3 设计原则

- **领域垂直**：旅行规划专用，双层领域校验（规则 + LLM）
- **可运营**：词库、拒答文案、天数范围集中在配置层
- **容错优先**：API 重试、中文友好报错、可选本地兜底模板
- **双端复用**：Web 聊天编排复用 CLI 领域层与业务层逻辑

---

## 二、运行条件

| 项目 | CLI | Web |
|------|-----|-----|
| Python | 3.8+ | 3.8+ |
| Node.js | — | 18+（前端构建/开发） |
| 包管理 | pip | pip + pnpm |
| 网络 | 可访问 DeepSeek API | 同左 |
| 密钥 | `.env` 中 `DEEPSEEK_API_KEY` | 同左 |

```bash
# .env（勿提交 Git）
DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

### 能力边界（CLI / Web 共用）

- 单城市 **1–7 天**短途规划
- 不接入实时机票/酒店/天气付费 API
- 不做用户登录与多租户（本地/单实例 SQLite）
- 输出为 AI 推断，出行前需核实官方信息

---

## 三、系统架构

### 3.1 整体分层

```
┌─────────────────────────────────────────────┐
│  表现层   frontend/ · presentation/         │  Web UI / CLI 展示
├─────────────────────────────────────────────┤
│  应用层   application/ · web/services/      │  流程编排
├─────────────────────────────────────────────┤
│  领域层   domain/                           │  领域守门 + 意图解析
├─────────────────────────────────────────────┤
│  业务层   services/                         │  规划生成 / 修订 / 导出
├─────────────────────────────────────────────┤
│  Prompt   core/prompts.py                   │
├─────────────────────────────────────────────┤
│  基础设施 core/ (config + llm_client)         │
└─────────────────────────────────────────────┘
```

### 3.2 CLI 交互状态机

```
[首规划态]
  │  自然语言输入
  ▼
领域校验 → 解析 city/days → 生成完整攻略
  │
  ▼
[追问态] ──Q──► 退出
  │
  ├─ Y / 「保存」──► 导出 storage/output/城市_x日游_YYYYMMDD.txt
  ├─ 追问微调 ────► 最小范围修订
  ├─ 显式换城 ────► 重新规划
  └─ 离题输入 ────► 拒答 + 引导
```

### 3.3 Web 页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/` | 首页 | 输入首条需求，创建会话并跳转聊天 |
| `/chat/:conversationId` | 聊天页 | 流式对话、暂停、分享、导出 |
| `/share/:token` | 分享页 | 只读预览会话或单条攻略 |

### 3.4 目录结构（当前仓库）

```
travel-plan-assistant/
├── main.py                      # CLI 入口
├── web_server.py                # Web 入口（uvicorn）
├── backend/
│   ├── core/                    # config · llm_client · prompts
│   ├── domain/                  # domain_guard · intent_parser · validator
│   ├── services/                # plan_service · export_service
│   ├── application/             # CLI assistant
│   ├── presentation/            # CLI formatter
│   ├── fallback/                # 本地兜底模板
│   └── web/
│       ├── api/                 # FastAPI 路由 · schemas · store(SQLite)
│       └── services/            # chat_service · share_service
├── frontend/src/
│   ├── app/                     # App.tsx · AppLayout
│   ├── features/
│   │   ├── home/                # 首页
│   │   ├── chat/                # 聊天、侧栏、输入栏
│   │   ├── share/               # 分享预览
│   │   └── travel-plan/         # 攻略解析与结构化展示
│   └── shared/                  # api · components · types · utils
├── storage/
│   ├── data/conversations.db    # Web 会话库
│   └── output/                  # 导出 TXT
└── docs/
```

---

## 四、核心业务流程

### 4.1 首规划（`plan_service.run_natural`）

1. **规则层** `domain_guard.validate_new_request`：离题词拒答
2. **解析层** `intent_parser.parse`：LLM 输出 JSON → `city` + `days`
3. **生成层** `plan_service.run`：单次 API 生成 9 模块完整攻略

### 4.2 追问修订（`plan_service.run_revise`）

1. **路由** `domain_guard.is_new_trip_request`：区分「微调」与「重新规划」
2. **修订** 基于当前攻略 + 用户反馈，**最小范围**改动
3. **输出** 展示调整说明；内部更新完整攻略供后续追问与保存

### 4.3 Web 流式对话（`chat_service.process_message_stream`）

1. 持久化用户消息至 SQLite
2. 复用 CLI 领域守门、意图解析、规划/修订逻辑
3. 通过 **SSE** 推送事件：`user_message` · `status` · `delta` · `done` · `error`
4. 前端 `fetch` + `ReadableStream` 消费（`AbortSignal` 支持客户端暂停）

**SSE 事件说明**

| 事件 | 载荷 | 用途 |
|------|------|------|
| `user_message` | Message | 服务端确认的用户消息 |
| `status` | `{ text }` | 规划进度提示 |
| `delta` | `{ content }` | 增量文本 |
| `done` | StreamDonePayload | 完成，含 has_plan/city/days/title |
| `error` | `{ message }` | 错误信息 |

### 4.4 暂停生成（前端）

- 生成中底部发送按钮切换为**暂停图标**，可点击
- 调用 `AbortController.abort()` 中断 fetch 流
- 已输出内容保留，消息标记 `stopped: true`
- **暂停会话不展示分享**（会话级与单条攻略级均隐藏），直至下次成功完成生成

### 4.5 保存与导出

| 方式 | 触发 | 输出 |
|------|------|------|
| CLI | 追问态 `Y` / 「保存」 | `storage/output/城市_x日游_YYYYMMDD.txt` |
| Web 服务端 | 追问态输入「保存」 | 同上 |
| Web 浏览器 | 攻略卡片「导出」按钮 | 本地下载 TXT |

### 4.6 分享

| 类型 | API | 预览 |
|------|-----|------|
| 会话分享 | `POST /api/conversations/{id}/share` | `/share/{token}` 展示全部问答 |
| 攻略分享 | `POST /api/conversations/{id}/messages/{msg_id}/share` | `/share/{token}` 展示单条攻略 |

分享 token 存于 SQLite `shares` 表；链接自动复制到剪贴板。

---

## 五、内容输出规范（产品定稿）

### 5.1 「不做 vs 要做」边界

| 维度 | 不做 | 要做 |
|------|------|------|
| **天气** | 不调用天气 API；不写实时气温/降水 | 季节与天气适配建议（穿衣、带伞等） |
| **景点票务** | 不查实时票价/余票 | 常识推断收费/预约，**单独汇总清单** |
| **交通购票** | 不查班次/可购票信息 | 大致交通方式与动线建议 |
| **路线** | 不做 GIS 计算 | 地理顺路、节奏合理、避免折返 |

### 5.2 完整攻略 9 大模块（顺序固定）

1. 行程概览  
2. 季节与天气适配建议  
3. 每日合理路线与攻略  
4. 美食推荐清单（Top 8）  
5. **景点预约与收费汇总清单**（独立章节，必含）  
6. 交通出行建议  
7. 注意事项与必备物品  
8. 预算参考  
9. 免责声明  

### 5.3 Web 结构化展示

前端 `travel-plan` 模块解析 Markdown 攻略：

- 按章节切分（概览、每日路线、美食等）
- 每日路线渲染为**时间轴**组件
- `@img[name|category|desc]` 标签解析为**关联图示**（上图下词）
- 完整攻略消息以文档卡片样式展示，附带分享/导出操作

---

## 六、领域守门（垂直领域）

### 6.1 双层校验

| 层级 | 模块 | 说明 |
|------|------|------|
| 规则层 | `domain_guard` | 词库匹配，零成本拦截明显离题 |
| 模型层 | `intent_parser` | JSON 字段 `travel_related=false` 时拒答 |

### 6.2 可配置词库（`config.py`）

- `TRAVEL_KEYWORDS` / `OFF_TOPIC_KEYWORDS`
- `REPLAN_KEYWORDS` / `FOLLOWUP_ADJUST_KEYWORDS`
- `DOMAIN_REJECT_MESSAGE` / `DOMAIN_GUIDE`

---

## 七、Web API 接口

### 7.1 会话

```
GET    /api/conversations
POST   /api/conversations          { "title": "新对话" }
GET    /api/conversations/{id}
PATCH  /api/conversations/{id}     { "title": "..." }
DELETE /api/conversations/{id}
```

### 7.2 聊天

```
POST /api/conversations/{id}/messages/stream
     Content-Type: application/json
     Accept: text/event-stream
     Body: { "content": "..." }

POST /api/conversations/{id}/export
```

### 7.3 分享

```
POST /api/conversations/{id}/share
POST /api/conversations/{id}/messages/{msg_id}/share
GET  /api/share/{token}
```

### 7.4 健康检查

```
GET /api/health
```

---

## 八、前端关键模块

| 模块 | 路径 | 职责 |
|------|------|------|
| `HomePage` | `features/home` | 创建会话、跳转聊天 |
| `ChatPage` | `features/chat/pages/Chat` | 流式对话、暂停、分享条件判断 |
| `ChatInputBar` | `features/chat/components/ChatInputBar` | 输入、发送/暂停切换 |
| `ConversationSidebar` | `features/chat/components/ConversationSidebar` | 会话列表、搜索、删除 |
| `ChatMessages` | `features/chat/components/ChatMessages` | 消息渲染、攻略卡片 |
| `PlanMessageActions` | `features/chat/components/PlanMessageActions` | 单条攻略分享/导出 |
| `TravelPlanContent` | `features/travel-plan` | 攻略结构化解析与展示 |
| `SharePage` | `features/share` | 分享链接只读预览 |
| `streamRequest` | `shared/api/client.ts` | SSE 流式请求与 AbortSignal |

---

## 九、异常与兜底

| 场景 | 处理 |
|------|------|
| 未配置 API Key | 启动时 `ensure_api_key()` 终止并提示 |
| 401 / 429 / 网络错误 | `llm_client` 重试 + 中文错误提示 |
| 生成失败 | 可选 `fallback/local_data.py`（成都/北京） |
| 解析失败 / 领域拒答 | 引导用户重输 |
| 修订未返回完整攻略 | 回退上一版攻略 |
| 客户端暂停 | `AbortError`，保留已生成片段，不触发分享 |
| 分享不存在 | 分享页展示「内容不存在或已失效」 |

---

## 十、部署说明

### 开发模式

```bash
# 后端
python web_server.py                    # :8000

# 前端
cd frontend && pnpm install && pnpm dev # :5173
```

### 生产模式

```bash
scripts\build-frontend.ps1            # 构建至 frontend/dist
python web_server.py                    # 托管静态资源 + API
```

生产环境建议设置 `UVICORN_RELOAD=0`。

---

## 十一、风险与后续扩展

### 11.1 风险

| 风险 | 缓解 |
|------|------|
| 模型幻觉 | Prompt 约束 + 免责声明 + 汇总清单结构 |
| API 限流/网络 | 重试 + 本地 fallback |
| 密钥泄露 | `.gitignore` + 文档警示 |
| 暂停后会话不完整 | 前端隐藏分享，引导用户继续对话 |

### 11.2 后续扩展方向

- 用户认证与云端同步
- RAG 接入本地权威攻略文档
- 多城市联程规划
- 分享链接有效期与访问统计

---

## 附录：关键模块职责速查

| 模块 | 职责 |
|------|------|
| `TravelAssistant` (CLI) | 主循环、路由、异常展示 |
| `chat_service` (Web) | SSE 编排，复用 plan_service |
| `share_service` (Web) | 分享 token 创建与读取 |
| `store` (Web) | SQLite 会话/消息/分享持久化 |
| `domain_guard` | 领域拒答、新行程/追问路由 |
| `intent_parser` | 自然语言 → 结构化参数 |
| `plan_service` | 生成与修订 |
| `export_service` | TXT 持久化 |
| `llm_client` | DeepSeek 统一网关 |

---

> CLI 运行：`pip install -r requirements.txt` → 配置 `.env` → `python main.py`  
> Web 运行：同上 → `python web_server.py` + `cd frontend && pnpm dev`
